<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<main>
  <h1>I changed it, <%= user ? user.profile.name : "David" %></h1>
  <% if (user?.profile.avatar) { %>
    <div>
      <img 
        src="<%= user.profile.avatar %>" 
        alt="<%= user.profile.name %> avatar"
      >
    </div>
  <% } %>
</main>

<%- include('./partials/footer') %>
